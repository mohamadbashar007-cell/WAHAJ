# Public contact address; do not place API keys in VITE_ variables.
VITE_CONTACT_EMAIL=
# Optional HTTPS endpoint accepting POST JSON and returning {"ok":true} only after acceptance.
VITE_CONTACT_ENDPOINT=
